import { ValidateCsvTimeRangeDirective } from './validate-csv-time-range.directive';

describe('ValidateCsvTimeRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateCsvTimeRangeDirective();
    expect(directive).toBeTruthy();
  });
});
